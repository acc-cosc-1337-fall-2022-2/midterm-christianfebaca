#include<iostream>
#include"question1.h"

using std::cout;
using std::cin; 
using std:: string;



int main(is_palindrome)
{
    int menu;

    cout<<"User menu for is_palendrome function"<<"\n";

    cin>>menu;
    
    //do while loop with if else if loop inside

    do
    {
    if(is_palindrome == "y" && is_palindrome == 'Y');
        cout<<"This is a palindrome"<<is_palindrome<<"\n";
    
        return true;
    
    else if(is_palindrome != 'Y' || is_palindrome != "y");
        cout<<"This is NOT a palidrome"<<is_palindrome<<"\n";
    {
        return false; 
    }
    
    auto letter = (' ');
    
    cout<<letter<<"\n";
    
    return 0;

    } while(menu != 'Y' || menu != 'y');
    cout<<"Program has been terminated"<<"\n";

    return 0;
}